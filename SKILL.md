---
name: animetrack-feishu
description: |
  Animetrack · 操作飞书多维表格制片表（默认模板「制片表 v4.1（公版）」，用户可改名 / 接入已有表）。当用户提到制片表、归档图片、改排期、加返修备注、查询进度、查完成度/完成率/进度百分比、催延期、开新项目、登记/分配团队、自动排期、兜底巡检、新成员入职等场景时使用。封装了全部字段 ID、阶段流转规则、13 个自动化清单、文件名归档启发式、团队配置 schema、合理排期算法、按难度加权的完成度算法与边界兜底逻辑。
---

# Animetrack · 飞书制片表 Skill

操作对象：飞书多维表格 `制片表 v4.1（公版）`，用户可能改名，按照用户指定的表格来进行操作。

> 用户用飞书 **Aily 智能体** 操作这张表。本 skill 让你（Claude）在收到口语化指令时，能**直接给出精确的字段 ID、操作步骤、必要参数**，省去和 Aily 反复来回沟通的成本。

> **首次启动**：每轮会话开始**先 read [`runtime.json`](runtime.json)**。`bound_base.app_token` 为空 = 还没绑定 base，必须按 [`BOOTSTRAP.md`](BOOTSTRAP.md) §3 走 bootstrap 流程（4 个问题串行问），**不要在没绑 base 时假装在操作数据**。

> **报告风格三原则**（所有给用户的回复——查询结果、Review、私信、巡检报告——都遵守）：
> 1. **短**：默认 ≤ 200 字 / 10 行；详情按需展开，结尾留一句「要看详细吗？」。
> 2. **平**：陈述事实，不渲染严重程度。说「剩 6 张 S/A 卡」，不说「⚠️ 紧急！还剩 6 张难卡」。**不用** ⚠️ ❌ 🚨 等告警 emoji。
> 3. **柔**：询问给默认值；提醒用「要不要…」「方便的话…」，不用「请尽快」「必须」「立刻」。一次问一个问题，不堆。

---

## 1. 表结构速查

- **Base 名称**: 制片表 v4.1 （公版）
- **Base ID**: 7621355715340324033
- **唯一数据表**: `tblxJGAeyRCOWOwa`（名称为 `Named it（on）`，使用时一般会被改名为剧名）
- **主键字段**: `fldG04l9e4`（序号 / Text）

记录定位优先级：先按**序号**(fldG04l9e4) 精确匹配，再退到 **场次镜头号**(fldp7RYAQ5)，再退到 **场次**(fldWXUk0q0) + 镜头描述 模糊匹配。

完整字段表见 [`fields.json`](fields.json)。下面只列高频字段：

### 1.1 标识/描述
| 字段 ID | 名称 | 类型 | 说明 |
|---|---|---|---|
| `fldG04l9e4` | 序号 | Text | 主键。**填 `S1C001` 这种格式**，不要填裸数字 `001`。S=Scene，C=Cut。 |
| `fldp7RYAQ5` | 场次镜头号 | Text | 形如 `S1C001`，通常和 `序号` 同值（冗余字段，方便筛选） |
| `fldWXUk0q0` | 场次 | 单选 | S1/S2/S3（id: `opt2IlOrSI`/`optVf3WAUO`/`optTNrmh5O`） |
| `fldFPhDe0K` | 模块 | Text | |
| `fld0kPSlzQ` | 镜头描述/备注 | Text | 写文字备注、返修说明用这个 |
| `fldR8qXhdV` | 原画难度 | 单选 | S/A/B/C |
| `fld4IlP6U6` | 镜头缩略图Filename | Text | 文件名文本 |

### 1.2 附件字段（图片归档目标）
| 字段 ID | 名称 | 阶段 |
|---|---|---|
| `fldSIiIAT4` | 镜头缩略图 | 通用预览 |
| `fldz0srXxF` | 参考（透视\book\美术\分配） | 前置参考 |
| `fldiy8ScAu` | 角色原画 | 原画 |
| `fld7RNntPG` | 清线 | 清线 |
| `fldhhN9HAA` | 绘景 | 绘景（背景/场景） |
| `fldnPwKUwx` | 上色 | 上色 |
| `fldGV1tR8j` | 返修内容 | 返修标注 |

### 1.3 阶段完成状态（单选）
| 字段 ID | 名称 | 选项 |
|---|---|---|
| `fldcVsh4PD` | 是否定稿（角色原画） | 待修改 / 已确定 |
| `fldvsnaOAf` | 角色原画已完成 | 已完成 / 未完成 |
| `fld1ZmZYQV` | 清线是否完成 | 已完成 / 未完成 |
| `fldFMjGeab` | 绘景是否完成 | 已完成 / 未完成 |
| `fldO4v4q8y` | 上色是否完成 | 已完成 / 未完成 |
| `fldPn4hCQG` | 3D/特效是否完成 | 已完成 / 未完成 |
| `fldpV90G6s` | 3D/非原画特效 | 未完成 / 已完成 / 无 |
| `fldxX2sOKx` | 合成 | 数字 0-2（红绿进度条） |
| `fldS0kbLGR` | **进度** | Stage 字段：原画 → 清线 → 上色 → 合成 → 已结束；旁路「返修中」 |

### 1.4 负责人（飞书人员字段）
| 字段 ID | 名称 | 多选 |
|---|---|---|
| `fldRWAydab` | 原画负责人 | 是 |
| `fldTRr7yhG` | 清线负责人 | 是 |
| `fldowZd2XP` | 上色负责人 | 是 |
| `fldVdXjCoQ` | 绘景负责人 | 否 |
| `fldoCuQ8ZQ` | 参考负责人 | 否 |
| `fld67smdxA` | 3D/非原画特效负责人 | 否 |
| `fldd2EjTTQ` | 合成人 | 否 |
| `fldS5E41DT` | 补充任务人员 | 否 |

### 1.5 排期字段（每阶段始 / 终）
| 阶段 | 始点字段 | 终点字段 |
|---|---|---|
| 角色原画 | `fldzH4Bq93` | `fldXynPjpD` |
| 绘景 | `fldj4s43RX` | `fldyoBo1Ey` |
| 清线/上色合并段（清上） | `fldUQ4Gukz` | `fldA0aSL1Y` |
| 上色 | `fldjQJNqEB` | `fldrNiULZx`（注意：这个字段是 **Text 类型**，不是日期） |
| 3D | `fld3OZPmN3` | `fldq1Yt2z3` |
| 合成 | `fld4vCQpsq` | `fld87th4Se` |

> 排期字段大多 dateFormat=`MM-dd`；上色始点 `fldjQJNqEB` 是 `yyyy/MM/dd`；上色终点 `fldrNiULZx` 是文本（历史遗留，建议照旧填字符串如 `2026/05/20`）。

### 1.6 自动维护字段（不要写）
| 字段 ID | 名称 | 类型 |
|---|---|---|
| `fldrkMHDAN` | 最后更新时间 | 系统 |
| `flde54MR7a` | 修改人 | 系统 |

### 1.7 视图（10 个）
- `vewfVd5nlJ` 概览视图（按场次分组）
- `vewKp7lvv2` 角色原画（按原画负责人分组，甘特图）
- `vew4RKF5XB` 参考
- `vewVcZExwV` 清线
- `vewpEV2bhu` 上色
- `vewTPDFzKe` 绘景
- `vewSusBDlR` 3D/特效
- `vewWSCYRVG` 合成（按合成人分组，按合成数字着色）
- `vewvvghRN3` 人员分配
- `vewG0tD9KH` 超级简单进度图

---

## 2. 阶段流转的核心规则

`进度`(fldS0kbLGR) 是 Stage 类型，`convertMode = "NextStage"`，按以下条件**自动**推进：

| 当前阶段 | 推进条件 | 触发字段 |
|---|---|---|
| 原画 → 清线 | 清线附件不为空 | `fld7RNntPG` |
| 清线 → 上色 | 上色附件不为空 | `fldnPwKUwx` |
| 上色 → 合成 | （由自动化「上色-合成自动化」触发） | |
| 合成 → 已结束 | （由自动化「合成-完成自动化」触发） | |

**重点**：把图片上传到下一阶段的附件字段，进度会自己往下走，**不要**手工去改 `fldS0kbLGR`，除非要标"返修中"(`optHJnvCo5`)。

---

## 3. 已部署的 13 个自动化（重要！避免重复设计）

### 3.1 到期催交 (datetimeField 触发)
| 触发字段 | 提醒文案 |
|---|---|
| `fldA0aSL1Y` 排期终点（清上） | 宝你的清上结束了吗要交付了 |
| `fldyoBo1Ey` 排期终点（绘景） | 宝你的场景结束了吗要交付了 |
| `fldXynPjpD` 排期终点（角色原画） | 宝你的原画结束了吗要交付了 |
| `fldq1Yt2z3` 排期终点（3D） | 宝你的3D结束了吗要交付了 |

> 注意：没有为「上色终点」(fldrNiULZx，文本字段无法触发) 和「合成终点」(fld87th4Se) 设催交。要么手动通知，要么补一个自动化。

### 3.2 阶段流转 (setRecord 触发)
- `wkfEV6BDOSB5L1Vn` **原画-清线** — 上传清线附件 → 自动推进进度、写排期始点、@清线负责人
- `wkfFCIjE5hp7vlLp` **清线-上色** — 上传上色附件 → 推进进度、@上色负责人
- `wkfpaC42ETLuZYP3` **上色-合成** — 上色完成 → 推进到合成、@合成人
- `wkfW9sC0rQyadiBM` **合成-完成** — 合成完成 → 标记已结束
- `wkfBLHp4eljiaF2Y` **更改进度的结果** — 进度变更后联动其他字段（含「返修中」分支）

### 3.3 飞书消息触发 (Aily 智能体问答)
- `wkfHL3tkPgDKDZuM` **项目进度查询** — Aily 接到「整体进度」类消息 → 找记录 → AI 总结 → 回复
- `wkfLjTI1aOPOKjlp` **进度追踪** — 单个镜头进度查询 / 低完成度镜头列表
- `wkfhrAn8X6jGfhUE` **进度查询反馈** — Aily 接到查询消息后回复"今天的更新记录及感谢语"

### 3.4 定时
- `wkfw1UINiG9ZXf8q` **进度查询反馈 定时触发** — 每日定时把更新记录推到群里

---

## 4. 高频操作 Playbook

### 4.1 用户传文件 → 自动判断归档位置

**判断流程**：

1. 解析文件名，匹配关键词 → 目标附件字段：
   ```
   原画 / 角色原画 / char / character           → fldiy8ScAu
   清线 / 线稿 / lineart / line                 → fld7RNntPG
   上色 / color / 着色                          → fldnPwKUwx
   场景 / 绘景 / bg / background / scene        → fldhhN9HAA
   参考 / ref / reference / 透视                → fldz0srXxF
   缩略 / 缩略图 / thumb / thumbnail / preview  → fldSIiIAT4
   返修 / rework / 修改                         → fldGV1tR8j
   ```

2. 解析文件名前缀定位记录。**标准命名**：`S{场次}C{镜头}` 系列，比如 `S1C001-清线.psd`、`S1C015_color.png`、`S2C034_bg.psd`：
   - **S** = Scene（场次） / **C** = Cut（镜头），是固定格式
   - 提取 `S{n}` → `fldWXUk0q0`（场次单选，对应 S1/S2/S3 选项）
   - 提取 `S{n}C{nnn}` 整体 → `fldp7RYAQ5`（场次镜头号），或直接当 `fldG04l9e4` 序号查
   - **兼容旧格式**：如果用户用 `S1-001` `S1_001` 等老格式，识别后照样匹配，但**回执时统一用 `S1C001` 标准格式**让团队习惯
3. **不要漏写文件名文本**：把原文件名同步写到 `fld4IlP6U6`（镜头缩略图Filename）

4. 上传后**不需要**手动改进度 / 完成状态 — 阶段流转自动化会处理。**例外**：上传到「绘景」字段不会自动推进主进度（绘景独立于主线），需要单独勾 `fldFMjGeab=已完成`。

**文件名歧义时**：直接问用户哪个镜头号，不要瞎猜。

### 4.2 修改排期时间

口语 → 字段映射：
- "把 S1C005 原画截止改到 5/20" → 找记录后写 `fldXynPjpD = "05-20"`
- "原画开工时间" → `fldzH4Bq93`
- "清上交付" / "清线交付" / "线稿交付" → `fldA0aSL1Y`（清上合并段终点）
- "上色截止" → `fldrNiULZx`（**文本，写 `"2026/05/20"` 形式**）
- "合成截止" → `fld87th4Se`
- "3D 截止" → `fldq1Yt2z3`
- "绘景截止" → `fldyoBo1Ey`

格式：日期字段值传 ISO 字符串或 `MM-dd`；上色终点是文本，照样字符串。

改完不需要重新触发提醒——`datetimeField` 触发器会按新日期自动安排提醒。

### 4.3 标记返修

返修不是简单写一句话，要触发返修流程：

1. **挂返修标注图**到 `fldGV1tR8j`（返修内容）
2. **写文字说明**到 `fld0kPSlzQ`（镜头描述/备注），格式建议 `[YYYY-MM-DD 返修] 具体内容`
3. **如果是角色原画返修**：把 `fldcVsh4PD` 设为 `待修改`（id: `optuysvpg4`）
4. **退主进度**：把 `fldS0kbLGR` 设为 `返修中`（id: `optHJnvCo5`）—— 「更改进度的结果」自动化会联动相关阶段完成状态。

### 4.4 发送延期/催交通知

**优先复用已有自动化**：4 个 datetimeField 自动化会在终点当天自动 @ 对应负责人。**不要重复发**。

**手动催的场景**（用户说"提醒一下 X 这个镜头延期了"）：
1. 先读对应阶段的负责人字段（见 §1.4）
2. 通过 Aily 的「发送飞书消息」节点私信该负责人（**不在群里 @**）
3. 消息模板（语气平和）：
   ```
   @{负责人} {fldp7RYAQ5} 的{阶段}原定 {终点字段值} 完成，
   现在过了 N 天。要不要同步个新时间？
   ```
4. 群通知**仅在用户明确要求**时才发，避免给团队压力

**全局延期检查**：让 Aily 跑「找出 `进度 != 已结束` 且 终点字段 < TODAY 的所有记录」→ 按负责人聚合 → 私信各人；只有全部超期才考虑群里发汇总。

### 4.5 进度查询（语义查询：到哪了）

直接复用 §3.3 的 3 个飞书消息触发自动化。**不要**自己写 FindRecord。
- 整体进度："项目进度怎么样" → wkfHL3tkPgDKDZuM
- 单镜头："S1C005 进度" → wkfLjTI1aOPOKjlp
- 今日更新："今天有什么更新" → wkfhrAn8X6jGfhUE

### 4.6 完成度统计（量化查询：百分之几）

用户问「完成度」「完成率」「做了多少了」「百分之多少」「干完了几成」→ 走 §10 的加权算法，**同时输出加权值和平均值**让用户对比。

---

## 5. 已知坑

1. **上色终点 `fldrNiULZx` 是 Text 不是 Date** — 写日期要传字符串，且无法被 datetimeField 自动化触发。
2. **绘景流不上主进度**：上传绘景不会推进 `fldS0kbLGR`，要单独维护 `fldFMjGeab`。
3. **`.base` 文件无法手动注入本地图片路径**：附件字段必须经由飞书云端 token 上传后写入；想批量灌图必须走 Aily / Open API。
4. **多负责人字段** (`fldRWAydab` / `fldTRr7yhG` / `fldowZd2XP`)：写值时记得是数组。
5. **里程碑** (milestoneMap)：DDL=`mlsGqRT4` (2026-01-22)、☠️=`mlsAgWxg` (2026-01-27)，硬交付节点。

---

## 6. 故障排查清单

- 上传图片后进度没动？→ 看是不是上传到了**当前阶段**字段而不是**下一阶段**字段；或是绘景流（不联动主进度）。
- 自动催交没发出？→ 检查终点字段是否是 Date 类型 + 是否非空；上色终点是 Text，本身就不会触发。
- 进度卡在「返修中」？→ 触发自动化的入口是手动改 `fldS0kbLGR`，要回主线得手动改回对应阶段。
- Aily 答非所问？→ 检查关键词是否命中 `wkfHL3tkPgDKDZuM` 的 SwitchBranch；可能要在 Aily 端补关键词。

---

## 7. 项目级生命周期

### 7.1 首次启动 / 接入已有表

**触发**：`runtime.json` 里 `bound_base.app_token` 为空，或用户第一次找你说话。

**完整流程**：[`BOOTSTRAP.md`](BOOTSTRAP.md) §3。一句话总结：

1. 检查 `runtime.json` → 没绑就进 bootstrap
2. **一次只问一个**，串行问 4 个：是新表还是已有表 / 项目叫啥 / 表链接发我 / DDL 啥时候
3. 抽 `app_token`，调 Open API 拿 schema，对照 `fields.json`
4. 字段全对上 → `schema_match = exact`；有差异 → 走 §13 自定义识别逐个吸收
5. 写 `runtime.json`，主表改名为项目名，更新 milestone DDL
6. 一句话回执：「绑好啦：《项目名》→ {url}。」

**强约束**：没拿到 `app_token` 之前**不要**假装能读写 base，不要瞎编数据。

### 7.2 开启新项目（旧数据归档 + 改名）

**触发**：用户说「开新项目」「这片做完了，下一部叫《XXX》」「重置制片表」等。

**前置必问**（不能瞎猜）：
1. 新项目叫什么名字？
2. 老数据是否要保留（默认保留）？
3. 团队人员是否沿用上一个项目（默认沿用）？

**执行步骤**（让 Aily 调用飞书 Open API）：

1. **归档老数据**：在同一 base 里用 `bitable.v1.app.table.create` 新建一张归档表，命名 `[旧 tableName] · 归档 YYYY-MM-DD`。把主表 `tblxJGAeyRCOWOwa` 的全部字段定义和记录复制过去（用 `batch_create` 分页拉记录 → 写到新表）。
   - 同时复制 `团队配置` 表为 `团队配置 · 归档 YYYY-MM-DD`（如果存在）
2. **改主表名**：`PATCH bitable.v1.app.table` 把 `tblxJGAeyRCOWOwa` 的 name 改为新项目名。**不要换 tableId**，否则 13 个自动化全废。
3. **清空主表数据**：`batch_delete` 主表所有 record，但**保留所有字段、视图、自动化、Stage 选项**。
4. **更新 milestoneMap**：把 DDL / ☠️ 两个里程碑日期问用户重设，原 ID 是 `mlsGqRT4` / `mlsAgWxg`。
5. **不要动**：自动化（13 个 wkf）、视图（10 个 vew）、字段定义（46 个 fld）、Stage 选项 ID。这些跨项目稳定。

**完成后输出**：归档表名、新项目名、清空记录数、保留的团队人数 — 让用户确认无误。

### 7.3 边界

- 用户没说项目名 → 问一句「新项目叫什么？」，不要瞎填。
- 旧 base 里已经有归档表 → 命名加序号 `归档 #2 YYYY-MM-DD`，不用问用户。
- 老项目还有未完成镜头 → 一句话：「老表还有 N 张没结束，归档后会脱离主线。要先收尾、搬到新项目、还是就这样归档？」三选一即可，不渲染严重。

---

## 8. 团队配置（人员名册）

### 8.1 配置表 schema

人员信息**存储在 base 内一张独立配置表 `团队配置`**（首次使用时让 Aily 创建）。这样 Aily 自动化 / 进度查询 / 分配脚本都能直接读，不用走外部存储。

字段定义（建议）：

| 字段名 | 类型 | 说明 |
|---|---|---|
| 姓名 | Text (primary) | |
| 飞书人员 | Person (single) | 用于 @ 提醒 |
| 职能 | 多选 | 选项: `参考` `角色原画` `清线` `上色` `绘景` `3D/特效` `合成` `导演` `制片` |
| 卡级偏好 | 多选 | 选项: `S` `A` `B` `C`；仅原画职能填，决定能接什么难度的卡 |
| 并发上限 | Number | 同时在手任务数上限，默认 2 |
| 周可用工时 | Number | 默认 5（一周 5 个工作日） |
| 加入日期 | Date | |
| 离场日期 | Date | 空=在职 |
| 备注 | Text | |

**首次创建**：让 Aily 调 `bitable.v1.app.table.create`。表名固定 `团队配置`，方便代码定位。

### 8.2 首次登记问询脚本

让 Aily 按这个顺序问用户，**一次问一类，不要堆**：

1. 「这个项目的**原画师**都有谁？分别能接哪些难度的卡（S/A/B/C）？」
2. 「**清线**负责人？」
3. 「**上色**负责人？」
4. 「**绘景**负责人？」
5. 「**3D/特效**负责人？」
6. 「**合成**负责人？」
7. 「**参考/导演/制片**？」（可选）
8. 「每人同时最多能接几张？默认 2，特别忙的人可以单独说。」

**多职能**：一个人可能挂多个职能（多选字段），比如「小明做清线也做上色」。

**默认**：未填卡级偏好 → 默认 `S/A/B/C` 全接。未填并发上限 → 2。

### 8.3 新成员入职

**触发**：用户说「小明加入了，他做清线」「新人 X 接 S/A 卡原画」。

**步骤**：
1. 检查 `团队配置` 表是否已有同名记录（按"姓名"去重）；有则更新职能/卡级，没有则插入。
2. 必须问到：飞书人员 ID（用于 @）、职能、卡级（如果是原画）。其他可省。
3. 写入后**主动报一句**：「小明已加入，职能=清线，并发上限=2。当前未分配任务数：N 张清线缺人。要现在分配吗？」

### 8.4 离场处理

用户说「X 离开项目了」：
1. 在 `团队配置` 写入"离场日期"；不要删行（保留历史归属）。
2. **巡检**：`X 当前在手未完成镜头` → 列出来，问要不要重派。
3. 重派完发私信给新接手人。

---

## 9. 自动分配 + 排期

### 9.1 分配规则（必须严格按此执行）

按工序遍历主表所有记录，对每个空缺的负责人字段：

| 工序 | 字段 | 选人规则 |
|---|---|---|
| 角色原画 | `fldRWAydab` | 按 `fldR8qXhdV` 难度过滤 `团队配置.卡级偏好` 包含该难度的人；多人时选当前在手任务最少的 |
| 清线 | `fldTRr7yhG` | 职能=清线 的人里负载最低的 |
| 上色 | `fldowZd2XP` | 职能=上色 的人里负载最低的 |
| 绘景 | `fldVdXjCoQ` | 职能=绘景 单选 |
| 3D/特效 | `fld67smdxA` | 职能=3D/特效 单选 |
| 合成 | `fldd2EjTTQ` | 职能=合成 单选 |
| 参考 | `fldoCuQ8ZQ` | 职能=参考/导演 |

**已有负责人的字段不要覆盖**（除非用户明确要求重派）。

**职能空缺**：某工序找不到任何人 → **停下来报告**「上色组无人」，让用户决定补人 / 转包 / 跳过。

### 9.2 合理排期算法

排期"合理"的定义：(1) 同镜头工序串行 (2) 不同镜头并行 (3) 单人不超并发上限 (4) 总工期 ≤ DDL  (5) 留 buffer 给返修。

**默认工时表**（工作日，1 日 = 8h）：

| 阶段 | S | A | B | C |
|---|---|---|---|---|
| 参考 | 1.0 | 0.5 | 0.5 | 0.5 |
| 角色原画 | 5 | 3 | 2 | 1 |
| 清线 | 3 | 2 | 1 | 0.5 |
| 上色 | 4 | 3 | 2 | 1 |
| 绘景 | 4 | 3 | 2 | 1 |
| 3D/特效 | 5 | 4 | 3 | 2 |
| 合成 | 1 | 1 | 0.5 | 0.5 |

> 难度字段 `fldR8qXhdV` 为空 → **默认按 B 卡处理并标记**，让用户在 review 时确认。

**工序 DAG**（依赖关系）：

```
参考(可选) ─→ 角色原画 ─→ 清线 ─→ 上色 ──┐
                                             ├─→ 合成
              绘景 ──────────────────────┤
              3D/特效 ───────────────────┘
```

**调度算法**：

1. **拉取 DDL**：从 milestoneMap 取 `mlsGqRT4`，没有就问用户要总交付日。
2. **倒推法**：按合成 → 上色/绘景/3D 三路并行 → 清线 → 原画 → 参考 倒着推。
3. **并发规则按难度分**（重要）：
   - **S / A / B 卡**：同一负责人**串行**，一张接一张，**不重叠**。第 N+1 张的开始日 = 第 N 张的结束日。
   - **C 卡**：是顺手活，**可堆叠并行**——同人手上的多张 C 卡可以全用同一时间段；C 卡也可以和 S/A/B 在同一时段叠（C 卡对关键路径长度不贡献）。
   - **甘特图直觉**：S/A/B 的甘特条**首尾相接**像火车厢；C 卡的甘特条**叠在同一段**像贴纸。
   - 排不下时（串行链超过 DDL）→ review 报告里轻提一句「按当前节奏 N 个工作日做不完，要不要：增加人手 / 推 DDL / 让某些卡降级 / 拆分给其他人」。
4. **跳过周末**：默认 5 工作日/周；如果用户配了周可用工时 ≠ 5 则按其值。
5. **Buffer**：在 DDL 前留 **15%** 总工期作返修缓冲；如果 buffer < 3 天直接预警。
6. **写字段**：按 §1.5 的映射，把 12 个排期字段全写上。上色终点 `fldrNiULZx` 是 Text，写 `yyyy/MM/dd` 字符串。

**举例**（张三的原画工序，同一负责人）：

```
任务清单：S1C001 (A 卡 3 天) + S1C002 (A 卡 3 天) + S1C003-S1C006 (4 张 C 卡 1 天)
错的排法（当前 bug）：所有 9 张都排 5/11 - 5/22 全叠在一起 ❌
对的排法：
  S1C001  ████░░░░  5/11 - 5/14（3 天）
  S1C002  ░░░░████  5/14 - 5/17（接续，3 天）  ← 与 S1C001 不重叠
  S1C003  ████░░░░  5/11 - 5/12（与 S1C001 同段叠，因为是 C）
  S1C004  ████░░░░  5/11 - 5/12
  S1C005  ████░░░░  5/11 - 5/12
  S1C006  ████░░░░  5/11 - 5/12
关键路径长度 = S1C001 + S1C002 = 6 天（C 卡不计入）
```

### 9.3 排期完成后的强制 review

排出来后**绝不直接说"完成"**，先给一份**短摘要**（≤ 10 行），细节按需展开：

```
【排期预览】
共 N 张（S{a}/A{b}/B{c}/C{d}），合计 X 工作日，DDL 前余 Z 天。
负载靠前：张三 12 张、李四 8 张。
要看的几处：
- 张三 1/15 那天 4 张 A 卡并行（A 卡应当串行）
- S1C008 难度未填，先按 B 估
- 合成暂无人

回「确认」我就落库；要调哪儿告诉我。要看完整明细吗？
```

**只有用户回「确认」**才正式写入 base。在此之前 dry-run，不用 ⚠️ 不用感叹号。

### 9.4 排期落库后的联络

落库成功后立即：

1. **私信每个负责人**（用 LarkMessageAction）：
   ```
   @{姓名} 这轮你这边 N 张：
   • S1C001 角色原画（S） 目标 1/15
   • S1C005 角色原画（A） 目标 1/18
   ...
   清线交付节点 1/20。有问题随时群里说一声。
   ```
   > 用「目标」不用「截止」；不写「请...」「必须...」。
2. **群里发汇总**：一段话——总周期、关键节点 1-2 个、风险（如果有）。**不要超过 5 行**。
3. **回访点**（可选自动化）：
   - 关键节点前 1 天**私信**当事人一句「明天是 X，方便对齐下吗」，**不发群**
   - 每周一发周度看板（复用 wkfw1UINiG9ZXf8q 的 cron 模式）

---

## 10. 完成度统计（按难度加权）

**触发**：用户问「完成度多少」「做了几成」「百分之几」「完成率」「干了多少了」「整体进度百分比」等。

**核心思想**：S/A 难卡占用产能多，C 卡再多也只是零头。**简单按镜头数平均会高估推进**——剩 5 张里有 4 张 S 卡时，看似还有 5/100=5% 没做，实际剩余工时可能 ≥ 25%。所以**默认输出加权值**，但**同时给出平均值供对比**，让用户看出"有没有把简单的先吃完、难的留到后面"这种结构性问题。

### 10.1 算法

参数全部存在 [`scheduling.json`](scheduling.json) 的 `completion` 段。三层计算：

**Step 1 — 单镜头完成度**（基于工时累加）

对每个镜头 `shot`：
```
diff      = shot.fldR8qXhdV   // S/A/B/C；空 → 默认 B（并标注）
total_w   = Σ stageDuration[stage][diff] for stage in 7 个工序
done_w    = Σ stageDuration[stage][diff] for stage in 已完成的工序
shot_pct  = done_w / total_w     // 0..1
```

工序"已完成"判定见 `completion.stageDoneRules`：

| 工序 | 完成条件（OR） |
|---|---|
| 参考 | `fldz0srXxF` 附件非空（可选工序，缺省视为完成） |
| 角色原画 | `fldvsnaOAf == 已完成` 或 主 Stage 已超过原画 |
| 清线 | `fld1ZmZYQV == 已完成` 或 主 Stage 已超过清线 |
| 上色 | `fldO4v4q8y == 已完成` 或 主 Stage 已超过上色 |
| 绘景 | `fldFMjGeab == 已完成`（独立轨） |
| 3D/特效 | `fldPn4hCQG == 已完成` 或 `fldpV90G6s ∈ {已完成, 无}` |
| 合成 | `fldxX2sOKx == 2` 或 主 Stage = 已结束 |

**返修中** 镜头：保留之前已完成阶段的工时，返修阶段本身不计入。

**Step 2 — 加权完成度**（项目级）

```
diff_w[d]    = 23 (S) | 16.5 (A) | 11 (B) | 6.5 (C)   // 该难度卡的总工时
weighted_pct = Σ (shot_pct × diff_w[shot.diff])  / Σ diff_w[shot.diff]
```

数学等价于：`weighted_pct = Σ done_w / Σ total_w`（按整个项目的工时池算，难卡的每个阶段自动占更大份额）。

**Step 3 — 平均完成度**（每镜头平等）

```
simple_pct = (Σ shot_pct) / N
```

**Step 4 — 差额诊断**

```
delta = weighted_pct − simple_pct
```
- `delta < 0`（加权低于平均）→ 简单卡先做了，**剩下的难卡是大头**，实际进度比表面慢
- `delta > 0`（加权高于平均）→ 难卡反而先啃了，**简单尾巴还没扫**，但风险已基本释放
- `|delta| < 1%` → 难易推进均衡

镜头数 < 3 时差额没意义，不输出 delta。

### 10.2 标准输出格式（默认短版，6 行内）

```
【完成度】项目 {tableName}
加权 68.4% / 平均 72.1% / 差额 -3.7%（剩下的偏难，实际比表面慢一点）
按难度：S 62.5% · A 80% · B 88% · C 100%
DDL 还有 7 个工作日。

要看阶段分布、风险清单、或某个人/工序的明细吗？
```

**用户说"详细点"才展开**长版（含按阶段表 + 风险段）：

```
按阶段：原画 5 / 清线 4 / 上色 3 / 合成 3 / 已结束 35 / 返修中 1
要看的几处：
- 剩余 S/A 卡 6 张，占剩余工时约 73%
- 日均节奏建议 ~1.2 张/工作日
- S1C008 难度未填，先按 B 估
```

**禁用**：⚠️ 🚨 ❌ 等告警 emoji；「紧急 / 必须 / 立刻 / 危险」等字眼；不要用感叹号。

### 10.3 按维度切片（可选）

用户问「张三完成度」「上色完成度」「S 卡完成度」时只算子集：

- **按人**：过滤所有 7 个负责人字段中包含目标人的镜头
- **按工序**：只统计该工序的 stageDuration 部分（而非整镜头）
- **按难度**：直接看 §10.2 表格里那一行
- **按场次**：按 `fldWXUk0q0` 过滤

### 10.4 边界

1. **难度未填** → 默认 B，但在报告里**显式列出**这些镜头让用户补。不要静默处理。
2. **总镜头 = 0** → 直接回「项目还没建任何镜头，无法计算」，不要返回 NaN/0%。
3. **单卡有错误状态**（比如 Stage = 上色 但 `fld1ZmZYQV == 未完成`）→ 按规则的 OR 取乐观值（Stage 推进通常意味着上一阶段已完成），同时在报告"风险"段标注数据不一致。
4. **D 卡**（schema 当前没有 D）：如果用户启用了 D 卡，建议权重设 3.5 并补 `stageDuration.D` 列；遇到未配置的难度等级先按 B 处理，并在报告里提一句让用户知道。
5. **小数精度**：百分比保留 1 位（`completion.displayPrecision`）；工时用 1 位小数。
6. **私信 vs 群发**：用户在群里问 → 群里回简短摘要 + "详细报告已私信"；私信问 → 直接发完整报告。

---

## 11. 兜底巡检（每日 / 按需）

巡检列表（由 Aily 每天定时跑一遍，或用户说「检查一下」时跑）：

| # | 检查项 | 处置 |
|---|---|---|
| 1 | 镜头无负责人 | 列出，问要不要批量分配 |
| 2 | 镜头无排期（始或终为空） | 列出，问要不要批量补排 |
| 3 | 排期反转（终点 < 始点） | 列出（数据问题，需要修） |
| 4 | 排期跨 DDL（任一终点 > DDL milestone） | 列出，问压缩还是推 DDL |
| 5 | 缺前置（已到清线但角色原画附件空） | 列出，可能是漏交 |
| 6 | 已过期未完成（终点 < 今天 且 完成状态 ≠ 已完成） | 私信负责人，**不发群** |
| 7 | 上色终点格式异常（`fldrNiULZx` 不是 yyyy/MM/dd） | 列出可修正项 |
| 8 | 长时间无更新（`fldrkMHDAN` > 3 天且未完成） | 私信负责人轻提醒 |
| 9 | 单人在某天负载超并发上限 | 列出，建议平移 |
| 10 | 团队配置缺岗（项目里某工序 0 人） | 提醒补员 |
| 11 | 新成员未入职登记（@人 在 `团队配置` 不存在） | 提示登记 |
| 12 | 序号格式错误（`fldG04l9e4` 不是 `S{n}C{nnn}` 格式，比如填了裸数字 `001`） | 列出建议批量修正为 `S1C001` 标准格式 |
| 13 | 同人同难度同时段并行（A 卡或更高，多张共享同一日期段） | 列出建议串行重排（按 §9.2 规则） |

**巡检报告默认输出**：给项目负责人**一条私信**，3-5 行摘要 + 一句「要看完整清单吗」。**不在群里 @ 任何人**。用户主动说「全员同步」才发群。

```
今日巡检（{date}）
- 3 张缺负责人、2 张缺排期
- 1 张排期反转（数据问题）
- 1 人今天负载略高

要看完整清单吗？
```

### 11.1 用户漏配工作流的兜底

如果发现 base 还没接上某个需要的工作流，主动提一句（**给三个选项 + 默认值**，避免开放式问）：

```
{一句话描述发现}。要不要我：
A. 加一个自动化（推荐）
B. 临时手动跑一次
C. 先记下来

不回也没关系，记下来留着。
```

排期完成后默认 3 天后回访一次（或关键节点前 1 天），回访只发一句私信：「上次排的 {项目}，方便看下进度吗？」

---

## 12. 通用兜底原则（最重要）

> 本 skill 的宗旨：**帮用户兜底 + 联络 + 人员分配**。当不确定时永远停下来问，不要替用户拍板。

1. **缺信息 → 问一句**：项目名、人员职能、卡级、DDL 缺了就问，但**一次只问一个**，不连珠炮。
2. **改基础结构前要确认**：建归档表、改主表名、清空记录、改 milestoneMap、批量改自动化 — 等用户回「确认」再动。
3. **批量写 base 前先 dry-run**：分配/排期/批量改时间，先给一份**短预览**（≤ 10 行），回「确认」后落库。
4. **报告短而平**（核心）：默认 ≤ 200 字 / 10 行；陈述事实，不渲染严重；不用 ⚠️ ❌ 🚨 等告警 emoji；不用「紧急 / 必须 / 立刻 / 请尽快」。结尾留一句「要看详细吗？」。
5. **私信优先**：负责人各自收清单，**默认不发群**。只有用户明确说「全员」「群里同步」才发群。
6. **离场/重派**：永远不删人，只标"离场日期"；离场前重派 ta 在手的镜头。
7. **难度未填默认 B**：在报告里轻提一句「S1C008 难度先按 B 估」让用户知情即可。
8. **失败时回滚**：批量写 base 中途失败，列已写部分，让用户决定继续 / 回滚。
9. **不主动制造焦虑**：不要算"还差 X 天就 DDL 了！"这种倒计时；只说"DDL 余 7 个工作日"让用户自己判断。
10. **加新功能走 §14**：用户说"加个 X" / "我想要 Y" → 先 Plan、再 Write、再 Test、等用户「保留」/「回滚」表态。**不允许**听一句就直接动；**不允许**做完才告诉用户。备份永远先做。

---

## 13. 自定义识别与吸收

> **核心思想**：SKILL 里记的是模板默认 schema，**用户随时会自己加字段、加选项、加视图、改名、加新表、加新自动化**。每次操作 base 都先对照 SKILL，发现差异就**先理解 → 再问 → 再回写 SKILL**，不要默默忽略，也不要默默按"我猜的意思"操作。

### 13.1 触发时机

下列任一情况**必须**走 §13.2 流程，不能跳过：

1. 读取记录时拿到 SKILL/`fields.json` 里没有的 `fldXXX` ID
2. 已知字段的 `name` 和 `fields.json` 对不上（用户改名了）
3. 已知 single/multi select 字段返回了 SKILL 没记的选项 ID
4. base 里多了 SKILL 没列的视图、表、自动化（wkfXXX）
5. 用户口头提到一个 SKILL 没出现过的字段/工序/角色名（"上次说的「分镜审稿」字段"）
6. 反向：SKILL 里记的字段在 base 里查不到了（可能被删/改名/合并）

### 13.2 处理流程（先猜后问再写）

**Step 1 — 先自己理解**

读字段名 + 类型 + 1-2 条样本值，给出一个**带把握的猜测**。例如：
- `fld新ABC 后期备注` type=Text，样本 "需要补一条对白" → 猜：「合成后期补充说明」
- `fld新DEF 海外版本` type=多选，选项 [`已交`,`待交`,`无需`] → 猜：「跨境发行的版本状态跟踪」

如果实在猜不到（字段名缩写、样本空），就老实承认「看不太懂」，进 Step 2 时直接问。

**Step 2 — 一句话问询**（带猜测，不要 open-ended）

```
我看 base 里多了一个字段「{name}」（{type}，比如 {sample}）。
猜可能是 {guess}？方便说一下具体用途吗？
```

如果是改名：
```
原来叫「{old}」(fld{id})，现在改成「{new}」了？还是同一个用途吧？
```

如果是新表 / 新视图：
```
看到一张新{表/视图}「{name}」，里面有 {字段或片段}。这是用来 {guess} 吗？
```

如果是新选项：
```
{字段名} 多了一个选项「{option}」。这是 {guess} 吗？
```

**一次只问一类**，发现多个差异就排队问，不要堆一长段。

**Step 3 — 拿到答复后回写 SKILL**

按差异类型动手：

| 差异类型 | 改哪里 |
|---|---|
| 新字段 | `fields.json` 加条目，含 `userNote` 字段记录用户原话；如该字段会进 playbook，在对应 §4.x 加一行索引 |
| 字段改名 | `fields.json` 改 `name`，旧名字写到 `aliases` 数组（用户口头还会用旧名） |
| 新选项 | `fields.json` 对应字段的 `options` 加一项 |
| 新视图 | `fields.json.views` 加一项；如视图带特殊筛选/分组逻辑，加 `userNote` |
| 新表 | 在 `fields.json` 顶层加一段 `extraTables`，结构同 `tableMap` |
| 新自动化 | `workflows.json` 加一项，标 `source: user_added` |
| 字段消失 | `fields.json` 加 `deprecated: true` 不要删（可能用户后悔加回来） |

**同时**追加一行到 [`customizations.md`](customizations.md)：
```
- {YYYY-MM-DD} | {差异类型} | {字段/视图/表名} | {用户解释} | {回写到哪个文件}
```

**Step 4 — 给用户回执**

```
懂了，已经记下来：{name} = {用户的用途解释}。
下次扫描就当固有字段处理，不再问。
```

### 13.3 边界

1. **不要边问边操作**：用户没回答用途之前，**只读不写**这个字段。万一用户操作里涉及这个字段，先把当前操作做了已知部分，再说「{这字段} 我还没搞懂用途，先没动它，方便说一下吗？」。
2. **猜测要老实**：把握 < 60% 就直说「看不太懂这是干嘛的」，别强行编一个"听起来很合理"的答案误导用户。
3. **批处理记忆**：同一类差异在一次会话里只问一次。比如同时新增 5 个字段都是"XX 后期备注"系列，就汇总问：「看到 5 个 `*后期备注` 字段，是按工序分别记备注吗？」
4. **遇到删除字段的情况**：不要直接从 fields.json 删条目，而是标 `deprecated: true`，原因记到 customizations.md。用户如果只是临时关掉/重命名，留个底好恢复。
5. **不要替用户做决定**：用户说"先随便记一下"——那就只记 customizations.md，**不要**自动加到 playbook（§4.x），等用户后续真的开始用了再升级。
6. **保护主 schema**：用户加的字段属于"自定义层"，永远不要把自定义字段的 ID 写进 §1.4/§1.5 等核心字段映射表，避免污染基线。新字段进 fields.json 时打 `source: "user_added"` 标签。

### 13.4 反向同步：SKILL 改了要看 base

如果用户在对话里说"我把 X 字段删了"「合并了 Y 和 Z」「加了个 W 视图给制片用」，**也要走 13.3 Step 3 + Step 4**，把变化回写到配置文件，不要只口头记住。

下次新会话会忘的，写下来才稳。

---

## 14. 新功能开发流程（Plan → Write → Test → Confirm → Rollback）

> 用户说「加个 X 功能」「我想要个 Y」「能不能让它自动 Z」「扩展一下 SKILL」时走这套流程。区分两类：
> - **A 类**：改 SKILL 本身（加章节、改算法、加 playbook）
> - **B 类**：改 base（加字段、加视图、加自动化、改 Stage 选项）
>
> 两类都走同样 5 步，区别只是动手对象。**绝不允许** "用户说一句就直接动" 或 "做完了才告诉用户"。

### 14.1 Step 1 — Plan（先输出计划，不写一行）

收到需求后**先停下**，输出一份**短计划**（≤ 12 行），按下面 6 项逐一回答：

```
【新功能计划】{功能名一句话}

目标：{用户为啥要这个，一句}
要改：{文件 / base 对象列表，3-6 行}
用户能感觉到的变化：{新指令 / 新报告字段 / 新自动化触发，2-3 行}
影响：{会动到哪些已有功能 / 不会动哪些}
怎么测：{1-2 个验证场景}
回滚成本：易 / 中 / 难（理由一句）

要这样做吗？或者要调哪儿？
```

**等用户回 OK / 确认 / 就这样**才进 Step 2。**不要**问「这样可以吗？」之外的开放性问题——计划不清楚就先回 Step 1 改计划。

### 14.2 Step 2 — Write（先备份再写）

1. **备份**：所有将被修改的 SKILL 文件先复制到 `.history/{filename}.bak.{YYYYMMDD-HHMMSS}`
2. **base 操作前生成 undo log**：写到 `.history/undo_{txnId}.json`，含每条记录的旧值、被改字段、操作类型
3. **再动手改文件 / 调 base API**
4. **一行总结**：「已改：SKILL.md §X / fields.json / base.字段Y。备份在 .history/{文件清单}」

**多文件原子性**：要改 N 个文件，**全部备份完**再开始改第一个。中途失败立即用备份回滚已改部分。

### 14.3 Step 3 — Test（用户面前跑一遍）

不允许只说"已实现"，必须给一个**可见的运行结果**：

- **A 类**（改 SKILL）：跑一遍假场景。"假设用户问 X，新版会输出这个：{示例}"。和旧版输出对比给用户看。
- **B 类**（改 base）：先在 1 条记录 dry-run，或在临时视图里测；不要直接全量动。"在 S1C001 上试了一次，效果是 {…}"

测试输出末尾："这样对吗？继续给所有镜头跑 / 不对要调？"

### 14.4 Step 4 — Confirm（强制等待，不主动收尾）

测试结果给出后，**必须等用户表态**。话术：

```
试用看看。
- 满意就回「保留」，我归档这次变更
- 不行就回「回滚」，恢复到改之前
- 也可以说要再调哪儿
```

**不要**主动说"那我先这样了"或"那就这么定了"——**沉默不等于同意**。如果用户隔了一段时间没回，下次会话再问一次「上次加的 X 还要留着吗」。

### 14.5 Step 5 — Rollback（任何时候都能撤）

用户说「回滚」「撤掉」「不要了」「换回去」：

1. 找最近一次该功能对应的 `.history/*.bak.*` 和 `undo_{txnId}.json`
2. **A 类**：用备份覆盖回当前文件
3. **B 类**：按 undo log 反向跑一遍 base 操作（删字段→恢复字段、改值→改回旧值）
4. **写一行**到 `customizations.md`：`{date} | revert | {功能名} | 用户要求回滚 | 恢复自 {bak_timestamp}`
5. **回执**：「已回滚到 {timestamp} 之前的状态。恢复了：{清单}。这次的备份还留着，要再启用直接说。」

**不要**真的删除备份文件——回滚后用户可能反悔再开。备份至少保留 30 天或最近 10 个版本（取多的）。

### 14.6 边界

1. **不能"假装实现"**：用户要的功能如果飞书 API 不支持 / SKILL 框架做不到，Step 1 就直说，不要硬上一个半残的版本。
2. **大功能拆步**：用户说"加一整套外发审批"——拆成最小 MVP 先跑通，每步走完一轮 5 步流程，不要一口气吃成胖子。
3. **SKILL 和 base 一起改时，先 SKILL 后 base**：SKILL 写错可以直接覆盖回；base 改了再回滚成本高（尤其涉及自动化 / 历史数据）。
4. **跨会话回滚**：每次会话开始时，如果用户说"回滚上次的"，先扫 `.history/` 找时间最近的几个备份，列出来让用户选，**不要**自己猜是哪一次。
5. **备份不要丢**：默认保留最近 10 个版本 + 30 天内全部；超出再清，清的时候输出清单告诉用户。
6. **回滚日志和自定义日志合并到 `customizations.md`**：type 用 `revert`，方便回看一条变更的"加—回滚—再加"完整轨迹。
7. **不要让用户记 txnId**：Step 2 生成的 txnId 是 SKILL 内部的；用户面前永远用人话「上次加的 XX 功能」「6/15 那次改动」。

### 14.7 和 §13 的关系

- **§13** 处理"用户已经在 base 里改了，SKILL 还不知道"——**事后吸收**
- **§14** 处理"用户口头要求加新功能"——**事前规划**
- 两边都写到 `customizations.md`，时间序排
- §13 的"先猜后问"和 §14 的"Plan"可以叠加：用户说"加个 X，类似上次那个 Y" → §14.1 Plan 阶段先猜 Y 是哪条变更（去 customizations.md 找），猜准了再写计划

---

## 15. 配套文件

- [`BOOTSTRAP.md`](BOOTSTRAP.md) — **首次接入指南**（用户视角导入流程 + agent 视角 4 步 bootstrap + runtime.json 格式 + 重导入处理）
- [`runtime.json`](runtime.json) — 当前会话绑定的 base 信息（`app_token`、项目名、DDL、schema_match 状态）；空 = 未绑定，agent 进 bootstrap
- [`fields.json`](fields.json) — 主表 46 个字段的完整定义（含选项 ID）；自定义字段进来时打 `source: "user_added"` + `userNote`
- [`workflows.json`](workflows.json) — 13 个自动化的精简描述；用户新加的标 `source: "user_added"`
- [`team.json`](team.json) — `团队配置` 表的字段 schema 模板（首次创建用）
- [`scheduling.json`](scheduling.json) — 默认工时表 + 工序 DAG + buffer 参数 + 完成度权重（`completion` 段，§10 引用）
- [`customizations.md`](customizations.md) — 自定义变更日志（§13 吸收 + §14 新功能流程产出，append-only，含 revert 类型）
- `.history/` — §14 自动产生的备份目录；`{name}.bak.{timestamp}` 文件 + `undo_{txnId}.json` 反向操作日志；保留最近 10 个版本或 30 天
- `制片表 v4.1 （公版） .base` — 飞书多维表格模板（用户用，agent 不读）；新项目时引导用户在飞书"新建多维表格 → 从 .base 导入"
- [`docs/glossary.md`](docs/glossary.md) — 中英双语术语对照（动画制作 / 飞书多维表格 / 本 skill 特定术语）
- [`docs/use-cases.md`](docs/use-cases.md) — 8 个真实场景对话示例
- [`llms.txt`](llms.txt) — AI 引擎索引文件（llmstxt.org 标准）
